[project]
name = "raif-lora"
version = "0.5.0-dev"
description = "RAIF v0.5 LoRA fine-tune (M4 Max / MLX)"
requires-python = ">=3.11,<3.14"
dependencies = [
  "mlx>=0.21",
  "mlx-lm>=0.21",
  "huggingface-hub>=0.27",
  "datasets>=3.2",
  "pyyaml>=6.0",
  "rich>=13.9",
]

[tool.uv]
package = false
