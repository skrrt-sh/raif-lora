# Python env
.venv/
__pycache__/
*.pyc
.uv-cache/

# Model weights / adapters (large binaries — regenerate or download)
models/
adapters/
adapters-cuda/
*.npz
*.safetensors

# Generated data + run artifacts (regenerate via src/make_data.sh).
# Ignore the whole tree, not just top-level *.jsonl — subdirs like
# data/archive/ and data/_pre_overhaul/ hold large local corpora/tarballs too.
data/
logs/

# Local agent / editor state
.claude/
